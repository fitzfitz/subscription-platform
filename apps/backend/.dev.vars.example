# Admin API Key for protected operations (e.g. creating/deleting plans if implemented)
# This is NOT the per-product API key.
ADMIN_API_KEY=your_super_secret_admin_key

# Optional: JWT Secret if adding clerk/JWT auth later
# JWT_SECRET=...
